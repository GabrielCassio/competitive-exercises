#include <iostream>
#include <random>

#define endl '\n'

int main () {

    std::ios::sync_with_stdio(false);
    std::cin.tie(0);

    

    int rand_num = 0;

    std::cout << "Saída aletaória: " << rand_num << "\n";

    return 0;
}