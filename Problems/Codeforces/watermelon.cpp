    #include <bits/stdc++.h>

    using namespace std;
    #define endl '\n'

    int main(){
        ios::sync_with_stdio(false);
        cin.tie(0);

        int i = 0; cin >> i;
        if ((i % 2 == 0) && (i > 2)) cout << "YES";
        else cout << "NO";

        return 0;
    }
